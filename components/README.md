# components/

공용 UI 컴포넌트.

가이드:

- 도메인 종속이 약한 공용 컴포넌트만 이곳에 둔다.
- 도메인 특화 컴포넌트는 `app/(dashboard)/<section>/_components/` 에 두는 것을 권장.
- 승인 카드 같은 공용 패턴은 `components/approvals/` 같이 서브폴더로 묶는다.

TODO: 기본 UI primitives, ApprovalCard, EmptyState 등.
