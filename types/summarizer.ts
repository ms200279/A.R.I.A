/**
 * 요약기 공용 입력/출력(비서·메일·문서에서 재사용).
 * 런타임 구현은 `lib/summarizers` 에 둔다.
 */
export type {
  ResourceKind,
  Summarizer,
  SummarizerAdapter,
  SummarizerInput,
  SummarizerOutput,
} from "@/lib/summarizers/types";
